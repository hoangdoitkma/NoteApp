<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.noteapp">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:label="NoteApp"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoteApp">
        <activity android:name=".ui.notes.EditNoteActivity" />
        <activity android:name=".ui.addnote.AddNoteActivity" />
        <activity android:name=".ui.expense.ExpenseDetailActivity" />
        <activity android:name=".ui.todos.AddTaskActivity" />
        <receiver android:name=".ui.todos.TaskNotificationReceiver"
            android:exported="true" />

        <activity android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.expense.AddExpenseItemActivity" />
        <receiver android:name=".ui.todos.ReminderReceiver"
            android:exported="true" />
    </application>
</manifest>
